{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run (LLDB) CMake target",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${command:cmake.launchTargetDirectory}",
        },
        {
            "name": "Run (cppvsdbg, Windows) CMake target",
            "type": "node",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${command:cmake.launchTargetDirectory}",
            "console": "integratedTerminal"
        }
    ]
}
